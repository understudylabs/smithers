## Classify upstream PR for the Python port

You're triaging a merged upstream PR to decide how (or whether) it
should be ported to `smithers_py`. Reply with exactly the JSON shape
described in the schema; no surrounding prose.

PR_NUMBER: {props.prNumber}
PR_TITLE: {props.prTitle}
PR_AUTHOR: {props.prAuthor}
PR_URL: {props.prUrl}
FILES_CHANGED: {props.filesChanged.join(", ")}
LABELS: {props.labels.join(", ")}

### Python target tree (existing files in the port)

When the action is `port` or `port-with-replacement`, populate
`pythonTarget` with the most plausible existing file from this list —
not a new scratch path. Pick by matching the upstream TS file's
responsibility to its Python counterpart. If no good match exists,
suggest a new path that fits the layout (e.g.,
`smithers_py/runtime/<feature>.py`).

Python tree (top {props.pythonTree.length} files):

{props.pythonTree.join("\n")}

### Decision categories (`action`)

- `port` — straightforward Python port (matching idiom)
- `port-with-replacement` — port but replace TS-specific tooling (Zod
  → Pydantic, drizzle-orm → SQLAlchemy, JSX → context manager)
- `skip-v0` — out of scope for v0.1 (gateway/server/sandbox)
- `skip-forever` — TS-only by nature (docs, `.d.ts`, bun init)
- `already-ported` — we already cover this on `port/resume`

### Hard rules

- `packages/gateway*`, `packages/server`, `packages/sandbox`,
  `packages/openapi`, `packages/devtools` → `skip-v0`.
- Docs-only changes → `skip-forever`.
- `.d.ts` files → `skip-forever`.

### Confidence

0-100 integer. < 60 → set `needsHumanReview: true`.
